# C01-T02 Complete-Packet Evidence

## Identity

- Task: `C01-T02 — Reconcile shared values, reports, and pinned sources`.
- Repository: `/Users/codex/Development/surgeist-generator`.
- Reviewed cycle plan: commit
  `2927c236220fb50db65727aa0088d0f2ee3c4475`, independently `CLEAN`.
- Semantic pair: commit
  `d3b3f8a783d7adfa2e0584af4a1f2f999c0bd0d4`, independently `CLEAN`.
- Implementation sequence: exact revision
  `5b2ee3cc01c237f64713925aaad83b14855690ab`, independently `CLEAN`.

## Exact Ordered Review Packet

The fresh reviewer must assess all three implementation commits together against
the final authorities; historical verdicts do not substitute for this review:

1. `5521d54071fa0940bde9dff3a437e40ed3ae6cd8` — initial shared contracts;
2. `d2980f7419215fe830ab915cbff8ceb1d9433413` — initial read-only containment;
3. correction span
   `2927c236220fb50db65727aa0088d0f2ee3c4475..e423b9abbd3433bd0cdb1719fe61cb6956e97a44`,
   whose sole commit is `e423b9abbd3433bd0cdb1719fe61cb6956e97a44`;
4. review-correction span
   `2feb11b110cbc6131ccf10c6f57a1ad6fa075853..cddd022cc6e2d01863a0af0f4d82b5a2191d6006`,
   whose sole commit is `cddd022cc6e2d01863a0af0f4d82b5a2191d6006`.

Resulting source head: `cddd022cc6e2d01863a0af0f4d82b5a2191d6006`.
The correction changes exactly Cargo lock/manifest, the permitted shared modules,
and two focused integration tests. It does not change artifact/lease code,
drivers, docs, plans, or `src/layout/legacy_generator.rs`.

## RED Evidence

RED was observed before the corresponding production correction:

- `cargo test --locked --offline -p surgeist-generator --no-default-features
  --test public_api` exited 101 because the exact result-returning report/count
  signatures and wire contracts were absent.
- The same command for `--test shared_contracts` exited 101 with four semantic
  failures: repeated source paths were rejected, the reserved coordination
  component was accepted, a symlink had the wrong kind, and a wrong extension
  was ignored.
- Source-focused unit runs exited 101 because the legacy cleanliness proof ran a
  configured filter sentinel, accepted disabled `config.worktree`, and accepted
  skip-worktree/assume-unchanged entries. Trusted-Git, snapshot, closing-hook, and
  protection-set tests initially failed to compile because those internal
  contracts did not exist.
- Precommit diagnostic REDs proved a dangling output ancestor returned `Ok`, an
  invalid checkout/escaped pinned source returned `SourceVerification` instead
  of `InvalidPath`, and a closing source-root replacement returned a successful
  `VerifiedSource`.

Each named RED is now covered by a targeted GREEN test in `src/core/source.rs`,
`src/core/corpus.rs`, `tests/public_api.rs`, or `tests/shared_contracts.rs`.

The first complete-packet review returned `NOT CLEAN` with four findings. Its
correction recorded additional genuine RED: an ending-zero commit produced the
same alleged wrong revision; mutation/helper-capable and malformed dynamic Git
argv passed first-token validation; and an injected device mismatch returned a
successful collection. The correction now flips a guaranteed-different nibble,
rejects every non-whitelisted complete argv before a zero-call executor, reaches
the device mismatch as `InvalidPath`, and removes deferred artifact/lease modules
and broad dead-code suppression from the T02 compile graph.

## Final Gate At The Resulting Source Head

All commands completed with exit 0 after the final correction:

```text
cargo generate-lockfile --offline
cargo check --locked --offline -p surgeist-generator --no-default-features
cargo test --locked --offline -p surgeist-generator --no-default-features
cargo clippy --locked --offline -p surgeist-generator --no-default-features --all-targets -- -F unsafe-code -D warnings
cargo check --locked --offline -p surgeist-generator --target wasm32-unknown-unknown --no-default-features --lib
cargo fmt --check
cargo deny --all-features --locked --offline list --format tsv --layout license
cargo audit --no-fetch --stale
```

- Tests: 30 unit + 5 public API + 6 shared contracts = 41 passed; doctests 0.
  Focused source and corpus subsets were respectively 17/17 and 7/7.
- Clippy used compiler unsafe-forbid plus warnings-deny and was clean.
- WASM compiled warning-free without installing a target.
- `cargo deny` produced only its expected absent-policy/default-config warning;
  the resolved license list exited clean.
- `cargo audit` loaded the already-present stale database (1,098 advisories),
  scanned 38 lockfile dependencies, and reported no vulnerability finding.
- The canonical owned-Rust unsafe scan found no executable match. `git diff
  --check` was clean. The generator worktree was clean after commit. Native
  evidence was Rust 1.97.0 on `aarch64-apple-darwin`.

Artifact hashes:

- `Cargo.lock`: `04f7074ae91cbd65600762b52b7f84e48d8a5521ad1d45f8b90b51a6807fe7ae`;
- `tests/public_api.rs`: `570e2cc5059fc87d6680138327e9cef1c91959fa55871c4d89195f7646b4cfd7`;
- `tests/shared_contracts.rs`: `0d9da46d70fe8075e936598e72894a436f3283bd4767a3ef09c6cb5baf455d3e`.

## Read-Only Boundary Evidence

- Immutable layout source `92054de23b7c4d431556ef7e42e2226dd1788f1f`
  lines 1–4626 and `src/layout/legacy_generator.rs` are each 4,626 lines,
  150,506 bytes, SHA-256
  `d2f5ca87cea6b36826e9172e2d7ba7a99196c375e2ca53f8a84a075200e70a9f`,
  and byte-identical. Live layout was observed only as clean HEAD
  `a6a6011aedb952572b8c0eac6f2a67b94c219f1a`; its unrelated drift was
  observational, not adopted.
- CSS remained at `ae44d858308e4f73c17e91c4c8768c43ce6ceb82`
  with only its recorded untracked `0644`, 156,722-byte remediation file at
  SHA-256 `08ba1dc26fd92faf7d95f562cb9639a9f9d40520b98992e6e5eef24083b3c1b6`.
- Root remained clean at `19590f6d9fa01c0df197c5ef07fb626c5cf18ced`.
- No sibling file was modified/formatted/tested, and no network, fetch, install,
  real corpus, browser, or acquisition path ran.

## Claim Boundary

This packet establishes C01 focused shared items 1–3 and 5 and the named shared
source/public-API prerequisites. It does not claim focused item 4 or 11 complete;
their executable closure remains C02/C03 under the reviewed sequence.
